# MLflow — self-hosted on AWS (see infra/aws/README.md), S3 artifact store
mlflow<3

# Agent orchestration
langchain
langgraph
langchain-community
liteLLM

# RAG / retrieval
llama-index
faiss-cpu

# Observability
langfuse

# Serving
fastapi
uvicorn[standard]
mcp

# Data / validation
pydantic
pandas
numpy
scipy
httpx
python-dotenv
scikit-learn
entsoe-py

# Power-curve kernel regression (analysis/efficiency.py:smooth_power_curve) — AMK only;
# ComparePCurve/FunGP from the same package were evaluated and not used, see README roadmap
dswe

# Time-series forecasting showcase (wind_prediction/)
statsmodels
torch
chronos-forecasting

# AWS (Bedrock LLM calls + DynamoDB session store — existing account)
boto3
